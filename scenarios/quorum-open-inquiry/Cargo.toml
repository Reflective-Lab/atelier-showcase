[package]
name = "example-quorum-open-inquiry"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
# Cross-workspace deps to Quorum + Axiom — path-patched at workspace root.
axiom-truth = { workspace = true }
quorum-truths = { workspace = true }
quorum-app = { workspace = true }
quorum-domain = { workspace = true }

# Standard runtime deps.
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }
anyhow = { workspace = true }
