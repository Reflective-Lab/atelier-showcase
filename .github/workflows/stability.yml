name: Stability

on:
  schedule:
    # Weekly Monday morning (6am UTC = 7am London)
    - cron: "0 6 * * 1"
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  RUST_VERSION: "1.96.0"
  REFLECTIVE_SIBLING_REF: ${{ github.head_ref || 'main' }}

jobs:
  bench-compile:
    name: Benchmarks (compile)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Checkout Reflective sibling dependencies
        run: bash scripts/ci/checkout-reflective-siblings.sh
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.RUST_VERSION }}
      - uses: Swatinem/rust-cache@v2
      - name: Install protobuf compiler
        run: bash scripts/ci/install-protobuf.sh
      - name: Ensure benchmarks compile
        run: cargo bench --workspace --no-run

  bench-run:
    name: Benchmarks (execute)
    runs-on: ubuntu-latest
    needs: bench-compile
    steps:
      - uses: actions/checkout@v6
      - name: Checkout Reflective sibling dependencies
        run: bash scripts/ci/checkout-reflective-siblings.sh
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.RUST_VERSION }}
      - uses: Swatinem/rust-cache@v2
      - name: Install protobuf compiler
        run: bash scripts/ci/install-protobuf.sh
      - name: Run benchmarks
        run: cargo bench --workspace
      - name: Extract baseline data
        if: always()
        run: |
          if [ -f scripts/extract-criterion-baseline.py ]; then
            python3 scripts/extract-criterion-baseline.py || echo "Baseline extraction failed"
          fi
      - name: Upload benchmark results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: bench-reports
          path: target/criterion/
      - name: Upload baseline data
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: baseline-data
          path: kb/Baselines/

  soak:
    name: Soak Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Checkout Reflective sibling dependencies
        run: bash scripts/ci/checkout-reflective-siblings.sh
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.RUST_VERSION }}
      - uses: Swatinem/rust-cache@v2
      - name: Install protobuf compiler
        run: bash scripts/ci/install-protobuf.sh
      - name: Run soak tests
        env:
          SOAK_CYCLES: "1000"
          SOAK_CONCURRENCY: "100"
          SOAK_ITERATIONS: "200"
        run: cargo test --workspace -- --include-ignored soak

  security-blocking:
    name: Security Audit (blocking)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Checkout Reflective sibling dependencies
        run: bash scripts/ci/checkout-reflective-siblings.sh
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.RUST_VERSION }}
      - uses: taiki-e/install-action@v2.85.6
        with:
          tool: cargo-audit
      - name: Run cargo-audit (blocking)
        run: bash scripts/ci/cargo-audit-blocking.sh

  deny:
    name: Cargo Deny
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Checkout Reflective sibling dependencies
        run: bash scripts/ci/checkout-reflective-siblings.sh
      # Not EmbarkStudios/cargo-deny-action: its docker container can't
      # install the repo's pinned musl toolchain and doesn't see the
      # sibling checkouts, so `cargo metadata` fails on sibling path deps.
      # Run cargo-deny in the runner env like the fleet's other repos.
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.RUST_VERSION }}
      - uses: Swatinem/rust-cache@v2
      - uses: taiki-e/install-action@v2.85.6
        with:
          tool: cargo-deny
      - name: Run cargo-deny
        run: cargo deny check
